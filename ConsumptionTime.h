#include <Windows.h>
#include "Controls.h"

#pragma once
class ConsumptionTime
{
	public:
		void DefineConsumptionTimeWindowControls(HWND hWnd, HINSTANCE hInstance);
	private:
		Controls controls;
};

